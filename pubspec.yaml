name: Seconds # <---------- THIS LINE IS CRUCIAL! Replace 'seconds_app' with your actual project name if it's different.
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '^3.5.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  geolocator: ^11.0.0 # Use the latest stable version
  shared_preferences: ^2.2.2 # Add this for local storage
  # emoji_picker_flutter: ^1.6.1 # Uncomment this if you want the bottom sheet emoji picker

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf